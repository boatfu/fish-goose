const config = {
    port: 2001,
    db: {
        database: 'fish_goose',
        username: 'root',
        password: 'root',
        port: '3306',
        host: 'localhost'
    }
}

module.exports = config;